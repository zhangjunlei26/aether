message Tick {}

actor Counter {
    state count = 0;

    receive {
        Tick() -> {
            count = count + 1;
        }
    }
}

main() {
    c = spawn(Counter());
    c ! Tick {};
    print("Actor spawned and message sent\n");
}

