// Test: unused variable warnings
// This file should compile but produce warnings for unused variables.
// The _prefixed variable should NOT produce a warning.

main() {
    used = 42
    println("used = ${used}")

    // _prefixed: intentional discard, no warning
    _ignored = 99

    println("Unused variable warnings test passed!")
}
