// Test program to demonstrate error reporting

message Process { value: int }

actor MyActor {
    state count = 0;

    receive {
        Process(value) -> {
            count = count + value;
        }
    }
}

main() {
    a = spawn(MyActor());
    a ! Process { value: 42 };
    print("Error suggestion test complete\n");
}
