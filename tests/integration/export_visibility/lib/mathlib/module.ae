// Public API — accessible via mathlib.double_it(), mathlib.PI
export double_it(x) {
    return internal_multiply(x, 2)
}

export add(a, b) {
    return a + b
}

export const PI = 3

// Private helpers — NOT accessible from outside
internal_multiply(a, b) {
    return a * b
}

const SECRET = 42
