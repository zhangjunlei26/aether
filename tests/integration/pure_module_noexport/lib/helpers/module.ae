// No export declarations — all symbols should be public (backwards compatible)

const LIMIT = 100

triple(x) {
    return x * 3
}

clamp(x) {
    if x > LIMIT {
        return LIMIT
    }
    return x
}
