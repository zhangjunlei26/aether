actor Counter {
    state int count = 0;
    
    receive(msg) {
        count = count + 1;
    }
}

main() {
<<<<<<< Current (Your changes)
    // Example: Using generated spawn_Counter() and send_Counter() functions
    Counter* c = spawn_Counter();
    send_Counter(c, 1, 0);
    Counter_step(c);
    print("Counter value after increment: %d\n", c->count);
=======
    // Note: spawn_Counter() and send_Counter() are generated by compiler
    // For now, this example just defines the actor (spawn/send usage in C code)
>>>>>>> Incoming (Background Agent changes)
    print("Actor simple assignment test\n");
}
