actor Counter {
    state count = 0;
    
    receive(msg) {
        count = count + 1;
    }
}

main() {
    // Actor defined - spawn/send functions coming soon!
    print("Actor working test\n");
}
