actor Counter {
    state count = 0;
    
    receive(msg) {
        count = count + 1;
    }
}

actor Printer {
    state messages = 0;
    
    receive(msg) {
        messages = messages + 1;
    }
}

main() {
    print("Multiple actors test\n");
}
