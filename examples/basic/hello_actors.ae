// Hello Actors - Basic actor example (simplified)
actor Counter {
    state count = 0;
    
    receive(msg) {
        count = count + 1;
        print(count);
    }
}

main() {
    print("Hello Actors example\n");
    // Actor defined but not spawned yet
    // Full actor runtime coming soon!
}
