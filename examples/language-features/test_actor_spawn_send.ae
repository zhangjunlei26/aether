actor Counter {
    state count = 0;
    
    receive(msg) {
        count = count + 1;
    }
}

main() {
    c = spawn(Counter());
    send_Counter(c, 1, 0);
    print("Actor spawned and message sent\n");
}

