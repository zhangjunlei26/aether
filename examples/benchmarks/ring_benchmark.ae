// Ring Benchmark (simplified)
actor RingNode {
    state messages_left = 0;
    
    receive(msg) {
        messages_left = messages_left + 1;
    }
}

main() {
    print("Ring benchmark\n");
}
