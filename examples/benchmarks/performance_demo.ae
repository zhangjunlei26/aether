main() {
    print("=== Aether Performance Demo ===\n");
    
    // Arithmetic test
    sum = 0;
    i = 0;
    while (i < 1000) {
        sum = sum + i;
        i = i + 1;
    }
    
    print(sum);
}
