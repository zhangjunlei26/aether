#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

int main() {
    {
printf("Hello from Aether!\n");
int x = 10;
int y = 32;
int sum = (x + y);
printf("Sum: %d + %d = %d\n", x, y, sum);
printf("Product: %d * %d = %d\n", x, y, (x * y));
    }
    return 0;
}
